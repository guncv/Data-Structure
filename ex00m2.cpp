#include <iostream>
#include <map>

int main(){
    std::ios_base::sync_with_stdio(false); std::cin.tie(0);
    int N,M,A,temp;
    std::vector<std::map<int,int>> m;

    std::cin >> N >> M >> A;
    for(int i = 0 ; i < N ; i++){
        std::cin >> temp;
        m[] = temp;    
    }
}